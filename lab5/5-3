#include <iostream>
int main() {
	int x;
	std::cin >> x;
	if (x >= 1 && x <= 100)
		std::cout << "OK";
	else std::cout << "Out of range";
}


//&& 로 조건문 범위를 설정해주고 else구문으로 범위 밖의 값이면 Out of range를 출력하게 하였다.
